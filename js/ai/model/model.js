/*
 * Placeholder for the trained neural network, overwritten automatically by
 * `node train/train.js` whenever a new best checkpoint is found.
 *
 * When null, js/ai/strategies.js's "trained" strategy falls back to the
 * baseline strategy stored in localStorage (see js/ai/training.js).
 */
let TRAINED_MODEL_DATA = null;
